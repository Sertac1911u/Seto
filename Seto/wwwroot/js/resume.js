// resume.js

document.addEventListener('DOMContentLoaded', () => {
    // İndirme Butonuna Tıklama Etkileşimi
    const downloadBtn = document.querySelector('.download-btn');

    downloadBtn.addEventListener('click', (e) => {
        e.preventDefault();
        // Gerçek bir dosya indirme bağlantısı eklediğinizde, aşağıdaki kodu güncelleyebilirsiniz
        // Örneğin: window.location.href = 'path/to/resume.pdf';
        alert('Resume download feature is not implemented yet.');
    });
});
